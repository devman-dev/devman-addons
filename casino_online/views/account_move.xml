<odoo>
    
    <!-- Acción para cuenta corriente casino -->
    <record id="action_casino_account_move_line" model="ir.actions.act_window">
        <field name="name">Cuenta Corriente Casino</field>
        <field name="res_model">account.move.line</field>
        <field name="view_mode">tree,form</field>
        <field name="view_id" ref="account.view_move_line_tree"/>
        <field name="domain">[('account_id.user_type_id.name', 'in', ['Receivable', 'Payable'])]</field>
        <field name="context">{
            'search_default_group_by_partner': 1
        }</field>
    </record>

    <!-- Vista tree personalizada (opcional) -->
    <record id="view_move_line_tree_casino" model="ir.ui.view">
        <field name="name">account.move.line.tree.casino</field>
        <field name="model">account.move.line</field>
        <field name="inherit_id" ref="account.view_move_line_tree"/>
        <field name="arch" type="xml">
            
            <!-- Simplificar vista mostrando balance en lugar de debit/credit -->
            <field name="debit" position="replace">
                <field name="balance" string="Importe" sum="Total"/>
            </field>
            <field name="credit" position="attributes">
                <attribute name="invisible">1</attribute>
            </field>
            
        </field>
    </record>

</odoo>