<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_company_form_bet_msgs" model="ir.ui.view">
        <field name="name">res.company.form.bet.msgs</field>
        <field name="model">res.company</field>
        <field name="inherit_id" ref="base.view_company_form" />
        <field name="arch" type="xml">
            <xpath expr="//sheet/notebook" position="inside">
                <page string="Casino">
                    <group string="Configuración Contable">
                        <group string="Cuentas Bancarias Custodia y operativa">
                            <field name="casino_deposit_journal_id" 
                                   options="{'no_create': True, 'no_open': True}"/>
                            <field name="casino_bet_transfer_journal_id" 
                                   options="{'no_create': True, 'no_open': True}"/>
                        </group>
                        <group string="Cuentas Contables (Opcional)">
                            <field name="casino_deposit_account_id" 
                                   options="{'no_create': True, 'no_open': True}"/>
                            <field name="casino_bet_account_id" 
                                   options="{'no_create': True, 'no_open': True}"/>
                        </group>
                    </group>
                    
                    <group string="Mensajes de Límites de Apuesta">
                        <!-- Campos comentados temporalmente para evitar errores hasta que se definan todos los campos necesarios -->
                        <!-- <field name="bet_msg_daily" placeholder="Has alcanzado tu límite diario." />
                        <field name="bet_msg_weekly" placeholder="Has alcanzado tu límite semanal." />
                        <field name="bet_msg_monthly" placeholder="Has alcanzado tu límite mensual." /> -->
                    </group>
                </page>
            </xpath>
        </field>
    </record>
</odoo>